Copy content to my clipboard using `pbcopy`.

If no instructions are provided, copy your last response. Otherwise, follow these instructions:

```
$ARGUMENTS
```

## Example

```bash
pbcopy << 'EOF'
[content to copy]
EOF
```
