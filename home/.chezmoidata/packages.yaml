# Homebrew packages managed by chezmoi
# Organized by functionality for easy maintenance

packages:
  darwin:
    brews:
      # Security & authentication
      - cosign
      - git-crypt
      - gnupg

      # Core CLI utilities
      - awscli
      - coreutils
      - curl
      - mise
      - pkl-lsp
      - tree

      # System monitoring
      - htop

      # Terminal multiplexer
      - tmux

      # System & certificates
      - ca-certificates
      - zsh

      # Docker & containerization
      - devcontainer
      - docker
      - docker-buildx
      - docker-completion
      - docker-compose
      - docker-credential-helper
      - docker-credential-helper-ecr

      # Media
      - ffmpeg
      - flac

      # Git
      - git
      - git-lfs

      # Documentation & diagramming
      - mermaid-cli
      - pandoc
      - texinfo

      # Compression utilities
      - lz4
      - lzo
      - xz
      - zstd
    casks:
      # Development tools
      - cursor
      - ngrok
      - visual-studio-code

      # Communication
      - slack

      # Productivity
      - obsidian
      - amethyst

      # Media
      - vlc

      # Security
      - 1password
      - 1password-cli

      # Audio
      - loopback

      # Fonts
      - font-fira-code-nerd-font

      # Terminal
      - ghostty

  linux:
    dnf:
      # Core system tools
      - ca-certificates
      - git
      - git-lfs
      - htop
      - tmux
      - util-linux-user
      - zsh
    apt:
      # Core system tools (Ubuntu equivalents of the dnf list)
      - build-essential
      - ca-certificates
      - git
      - git-lfs
      - htop
      - libssl-dev
      - passwd
      - pkg-config
      - tmux
      - zsh
